langcode: en
status: true
dependencies:
  config:
    - user.role.audit_manager
  module:
    - user
id: user_add_role_action.audit_manager
label: 'Add the Audit manager role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: audit_manager
