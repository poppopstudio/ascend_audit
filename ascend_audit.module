<?php

/**
 * @file
 * Contains hook implementations for the Ascend audit module.
 */

use Drupal\ascend_audit\Hook\AuditHooks;
use Drupal\ascend_audit\Hook\EntityHooks;
use Drupal\Core\Entity\EntityInterface;
use Drupal\Core\Form\FormStateInterface;
use Drupal\Core\Hook\LegacyHook;
use Drupal\Core\Render\Element;

/**
 * Legacy hook implementation.
 *
 * @todo Remove this method when support for Drupal core < 11.1 is dropped.
 */
#[LegacyHook]
function ascend_audit_user_presave(EntityInterface $entity) {
  \Drupal::service(EntityHooks::class)->userPresave($entity);
}

/**
 * Legacy hook implementation.
 *
 * @todo Remove this method when support for Drupal core < 11.1 is dropped.
 */
#[LegacyHook]
function ascend_audit_school_update(EntityInterface $entity) {
  \Drupal::service(EntityHooks::class)->schoolUpdate($entity);
}

/**
 * Legacy hook implementation.
 *
 * @todo Remove this method when support for Drupal core < 11.1 is dropped.
 */
#[LegacyHook]
function ascend_audit_config_pages_insert(EntityInterface $entity) {
  \Drupal::service(AuditHooks::class)->configPagesInsert($entity);
}

/**
 * Legacy hook implementation.
 *
 * @todo Remove this method when support for Drupal core < 11.1 is dropped.
 */
#[LegacyHook]
function ascend_audit_config_pages_update(EntityInterface $entity) {
  \Drupal::service(AuditHooks::class)->configPagesUpdate($entity);
}

/**
 * Legacy hook implementation.
 *
 * @todo Remove this method when support for Drupal core < 11.1 is dropped.
 */
#[LegacyHook]
function ascend_audit_entity_type_alter(array &$entity_types) {
  \Drupal::service(AuditHooks::class)->entityTypeAlter($entity_types);
}

/**
 * Legacy hook implementation.
 *
 * @todo Remove this method when support for Drupal core < 11.1 is dropped.
 */
#[LegacyHook]
function ascend_audit_form_user_cancel_form_alter(&$form, FormStateInterface $form_state, $form_id) {
  \Drupal::service(EntityHooks::class)->formUserCancelAlter($form, $form_state, $form_id);
}


/**
 * These two functions were required in order to add compatibility with
 *  field_group module. This also necessitated the audit.html.twig template.
 *  https://www.drupal.org/project/field_group/issues/2859858
 */

function ascend_audit_theme($existing, $type, $theme, $path) {
  return [
    // This is for the field_group workaround above.
    'audit' => [
      'render element' => 'elements',
    ],
    // This is for the auditor working info block.
    'auditor_working_info' => [
      'variables' => [
        'school' => NULL,
        'year' => NULL,
      ],
      'template' => 'block--auditor-working-info',
    ],
  ];
}

function template_preprocess_audit(&$variables) {
  $variables['view_mode'] = $variables['elements']['#view_mode'];

  // Helpful $content variable for templates.
  $variables += ['content' => []];
  foreach (Element::children($variables['elements']) as $key) {
    $variables['content'][$key] = $variables['elements'][$key];
  }
}

// Get Audit entity edit forms handled by gin (to get sidebar).
function ascend_audit_gin_content_form_routes(): array {
  return [
    'entity.audit.add_form',
    'entity.audit.edit_form',
    'entity.audit.add_form.auditor',
    'entity.audit.edit_form.auditor',
    // Also in resource module but no harm done.
    'entity.taxonomy_term.edit_form',
  ];
}

/**
 * Implements hook_preprocess_page().
 * Show route details for dev/debugging.
 */
// function ascend_audit_preprocess_page(&$variables) {
//   $route_name = \Drupal::routeMatch()->getRouteName();
//   $route_parameters = \Drupal::routeMatch()->getRawParameters()->all();

//   // Add to page variables for use in templates
//   // $variables['current_route'] = $route_name;
//   // $variables['route_parameters'] = $route_parameters;

//   // Or show as messenger
//   \Drupal::messenger()->addStatus('Route: ' . $route_name);
// }
